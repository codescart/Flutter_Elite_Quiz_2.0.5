import 'package:flutter/material.dart';
import 'package:flutterquiz/app/app.dart';
import 'package:flutterquiz/test1.dart';

//Elite quiz - v-2.0.5

void main() async {
   runApp(await initializeApp());
  // runApp(await MyHomePage(title: 'Phone pay',));
}
